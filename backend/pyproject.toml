[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "backend"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "pandas",
    "yfinance",
    "pymongo"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["*"]